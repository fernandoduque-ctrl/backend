# Build ideal: yarn && yarn build:deploy. No primeiro deploy (0 usuários), start-prod roda seed automático; defina SKIP_AUTO_SEED=true se não quiser.
services:
  - type: web
    name: folha-wizard-api
    runtime: node
    buildCommand: yarn && yarn build:deploy
    startCommand: yarn start:prod
