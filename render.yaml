# Build ideal: yarn && yarn build:deploy. Só "yarn" força compile no start (mais lento); o script usa stub HTTP na PORT durante o build para o health check da Render.
services:
  - type: web
    name: folha-wizard-api
    runtime: node
    buildCommand: yarn && yarn build:deploy
    startCommand: yarn start:prod
