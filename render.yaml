# Build DEVE incluir nest build (yarn build:deploy). Só "yarn" não gera lib/ e o start falha.
services:
  - type: web
    name: folha-wizard-api
    runtime: node
    buildCommand: yarn && yarn build:deploy
    startCommand: yarn start:prod
