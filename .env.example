# SQLite: caminho relativo à pasta prisma/. Se a API retornar "table main.User does not exist", rode: npm run db:push && npm run db:seed
DATABASE_URL="file:./dev.db"
JWT_SECRET="troque-em-producao-use-string-longa-e-aleatoria"
JWT_EXPIRES_IN="7d"
PORT=3001
STORAGE_PATH="./storage"
